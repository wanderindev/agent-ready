# Agent-fixes prompts index

One file per autonomous-fix brief produced by the `fix-issue` skill (`.claude/skills/fix-issue/`). Each brief is the contract for one agent run against one GitHub issue. Numbered by issue, not by session.

This continues the prompt-preservation convention (`docs/methodology/conventions.md`) past the Phase 2 experiment. The Phase 2 experiment corpus lives at `docs/phase-2/prompts/` (sessions 01-21, the F-1 through F-4 runs); ongoing production fix-work briefs live here.

Format: `- [fix-issue-NNN.md](fix-issue-NNN.md) — #NNN: <title>. <one-line note on shape/outcome>.`

---

- [fix-issue-103.md](fix-issue-103.md) — #103: extract duplicated research-validation logic into `research_common.py`. Pure mechanical backend refactor (one `_generate_references_section` is byte-identical across the two modules); thin wrappers preserve the public signatures so the two callers stay untouched.
- [fix-issue-11.md](fix-issue-11.md) — #11: apply auto-fixable ruff + `ruff format` to `backend/app/`, then flip the CI ruff step to blocking. Issue numbers drifted (83→73 errors, ~10→2 residual); brief defers the 2 stragglers (F841, E402) via line-level `# noqa` rather than hand-fix, and corrects the count.
- [fix-issue-192.md](fix-issue-192.md) — #192: test coverage for admin dashboard endpoints (dashboard.py). Brief enumerates ~35 uncovered endpoints across 8 entity groups (stats/suggestions/research/articles/tags/status/admin-emails/pricing); mirrors `test_media_library.py` (real `admin_token` fixture) and the `test_article_generation.py` mocking style; corrects the issue's "27% / 430 uncovered lines" against the now-1697-line file and lists the 5 already-covered endpoints to skip.
- [fix-issue-193.md](fix-issue-193.md) — #193: unit tests for edu/research content-generation services. 6 new test files (prompts, research_summary, research_common, suggestion_generation, edu_material_generation, edu_admin); brief verifies patch targets per service (research_summary uses `anthropic.Anthropic` directly, others use `_get_client`); scopes `edu.py` to admin CRUD only and explicitly defers upload/download/generate paths; enforces the oracle rule (no characterization tests).
- [fix-issue-194.md](fix-issue-194.md) — #194: test coverage for public read endpoints (search, public_media, categories, zones). 4 new test files, 13 endpoints total; oracle is the Pydantic schema contracts. Surfaces a latent routing-order bug in `categories.py` (`/{category_slug}` declared before `/taxonomies` shadows the taxonomies route) — agent characterizes the 404 in a test and flags as a follow-up rather than fixing in this PR (shape #4).
- [fix-issue-195.md](fix-issue-195.md) — #195: test coverage for pure-logic & parser services (media_scoring, loc, wikimedia, markdown, slug). 3 new test files (slug, markdown, media_scoring) plus extensions to `test_media_library.py` for uncovered loc/wikimedia surface (upsert merge contract, crawl orchestrators, `get_best_image_url`); optional `test_image_storage.py` for pure-bytes helpers only. Brief encodes deterministic input→output oracles for slug/markdown.
- [fix-issue-17.md](fix-issue-17.md) — #17: bump GitHub Actions Node 20→22 (deprecation cutoff June 2026). One-line edit (`.github/workflows/ci.yml:83`, `node-version: '20'`→`'22'`); brief corrects two issue-body claims against source — there is only one workflow file/one site (not "all occurrences"), and the npm cache is keyed off the lockfile hash, not the Node version (no lockstep change needed).
- [fix-issue-55.md](fix-issue-55.md) — #55: tighten dev-token admin bypass — require `debug AND environment == "development"` (new `environment: str = "production"` Settings field) plus WARN log + Sentry breadcrumb when the bypass fires. Brief corrects a key issue-body drift (shape #2): prod safety comes from the `debug` default + `.env`, NOT an inline `docker-compose.prod.yml:30 DEBUG=false` (line 30 is the frontend port; the prod backend has no inline DEBUG), so the only compose change is adding `ENVIRONMENT=development` to `docker-compose.yml` for local dev — `docker-compose.prod.yml` is untouched. Scoped to option 1 only (not the "remove entirely" alternative, which needs a human decision).
- [fix-issue-56.md](fix-issue-56.md) — #56: re-check the admin allowlist when validating magic-link tokens — add `.join(AdminEmail, AdminEmail.email == MagicLink.admin_email)` + `.filter(AdminEmail.is_active.is_(True))` to `validate_admin_token` so a removed/deactivated admin's token stops working immediately instead of surviving its 24h window. All ORM identifiers verified accurate against `models/magic_link.py`; canonical filter mirrors `auth.py:103-108`. Drift note: `verify_token` (the HTTP `/auth/verify-token` endpoint) is a SEPARATE query that does not call `validate_admin_token`, so existing tests are unaffected. #54's dependency-refactor and the optional write-time `used_at` cleanup are both explicitly OUT of scope. NOTE: file-overlaps #55 on `dependencies.py` + `test_auth.py` — the two PRs will conflict at merge; resolve the second with `/update-pr`.
- [fix-issue-60.md](fix-issue-60.md) — #60: stop `EducatorAuthContext` granting access from stale localStorage on a network error — the mount-effect catch branch was calling `setHasAccess(true)` from cached data; replace with `setHasAccess(false)` + a new `verifyError` context flag, surface a "can't verify your access" message in `EducatorAccessGate`'s login view, add a `verify_unavailable` i18n key (es/en). Frontend-only (4 files), no test infra (lint+build verification only), production-touch none. No issue-body drift; mirrors the gate's existing error-message pattern (`EducatorAccessGate.jsx:152`).
- [fix-issue-12.md](fix-issue-12.md) — #12: migrate Pydantic v1 `class Config` → `model_config = ConfigDict(...)` in `schemas/article.py` + `category.py`. Mechanical block-form rewrite of 3 `Config` blocks + one import. Drift (shape #2): issue overstated the work — source has NO `orm_mode`/`@validator`/`allow_population_by_field_name` (already at v2 attr names); only the block form remained. `core/config.py`'s BaseSettings `Config` is hard-fenced OUT (needs `SettingsConfigDict`, different semantics).
- [fix-issue-69.md](fix-issue-69.md) — #69: stop `image_storage.download_image` buffering the full body before the 20MB cap check (worker-OOM, `code-quality:critical`). Rewrite to `httpx` `client.stream(...)` + `iter_bytes()` accumulator that aborts mid-stream once `MAX_DOWNLOAD_SIZE` is exceeded; return contract `tuple[bytes, str]` preserved (`bytes(chunks)`, not bytearray) for the sole caller `download_and_upload`. No drift — every line/constant matched. Production-touch none (the `:critical` is stability, not a data path).
- [fix-issue-77.md](fix-issue-77.md) — #77: make LLM JSON parsing consistently defensive. Scoped a vague "inconsistent across services" issue down to ONE shared `parse_llm_json` helper in `research_common.py` + exactly 3 migration sites (article_generation ×2, suggestion_generation ×1); 2 conformant exemplars left alone. Drift (shape #2): issue's cited `research.py`/`edu_research.py` guarded-parse sites don't exist (they only return `.text`), and the `messages.create`-wrapping / new `llm.py` is hard-fenced to #76. Pre-resolved the `.type` test-mock trap (type-less article mocks vs thinking-block suggestion responses) with a `getattr(b,"type","text")` filter.
- [fix-issue-84.md](fix-issue-84.md) — #84: replace the bare `except Exception: pass` swallowing `paypal.cancel_invoice` failures in `orders.py` `renew_payment_link` with log + `sentry_sdk.capture_exception`. PAYMENTS PATH — brief BINDS to observability-only (no re-raise, invoice-clearing control flow byte-for-byte unchanged) so production-touch stays none; a control-flow change would be production-touch YES. Drift: line 501-504 not 495-498; function is `renew_payment_link` not admin-edit; and the issue's `HTTPStatusError`/422 fix snippet was REJECTED (shape #2 — `cancel_invoice` returns a bool, never calls `raise_for_status`). NOTE: file-overlaps nothing in this batch.
- [fix-issue-85.md](fix-issue-85.md) — #85: narrow `media_scoring.score_candidates`'s run-aborting broad `except Exception: break` into transient(`continue`)/malformed(`continue`+Sentry)/terminal(`break`+Sentry) handlers; unexpected exceptions propagate. Caught a latent hazard the issue's own `continue` snippet would introduce — the batch query has no offset, so `continue` after rollback re-fetches the same NULL-score rows forever; pre-resolved with `.offset(skipped_batches * batch_size)` + a `batches_skipped` field added to `stats` and `ScoreResponse`. Drift: except at 121-124 not 117-120; cited `research.py`/`edu_research.py` sibling pattern doesn't exist.
- [fix-issue-139.md](fix-issue-139.md) — #139: mount `/var/run/docker.sock` into the DEV backend service in `docker-compose.yml` so testcontainers-backed pytest runs via `docker-compose exec`. Corrected the issue's "one-line change" to TWO lines (sock volume + `user: "0:0"`) because `backend/Dockerfile` runs `USER appuser` (uid 1000) which can't read the root:docker 660 socket. Drift: prod compose ALREADY mounts the sock — on `certbot`/`watchtower`, not backend. `docker-compose.prod.yml` HARD-FENCED OUT (sock-in-prod-backend = security regression). Verification via isolated `-p agent-issue-139` + non-committed port-override, never disturbing the operator's running stack.
- [fix-issue-79.md](fix-issue-79.md) — #79: translation.py — expose DeepL `formality` as a typed `Formality = Literal[...]` param threaded through both public + both `_sync_*` methods (default `"default"` preserved so `admin.py`/`dashboard.py` callers are untouched), and make `_protect_code_blocks` regex attribute-tolerant. Drift (shape #2): issue line numbers off-by-one (formality literals at 73/93 not 74/94) and its regex snippet injects `class` BEFORE existing attrs → duplicate `class`; brief corrects to inject AFTER. Backend service-layer only, DeepL mocked, production-touch none.
- [fix-issue-80.md](fix-issue-80.md) — #80: migrate `mailing_list._send_confirmation_email` from raw f-string HTML to Jinja2 autoescape templates (two new `subscribe_confirm.{es,en}.html`), mirroring the #31 pattern in `notifications.py` (`_env`/`_render` at lines 39-47) + `admin_magic_link.html`. Drift (shape #2): issue said func at 117-157 returning `bool`; source is 32-79 returning `None` and raising `EmailDeliveryError` — brief preserves the `-> None` no-return contract (callers don't inspect a return). Only interpolated value is the server-controlled `confirm_url`; production-touch none. No overlap with #79.
- [fix-issue-61.md](fix-issue-61.md) — #61: add structured `logger.warning` + Sentry breadcrumb at 6 auth-failure sites (admin-token validate, magic-link not-found/expired, admin-login enumeration no-op, educator verify-code email-not-found/invalid-code), mirroring the canonical dev-token-bypass pattern at `dependencies.py:41-47`. LAYER 1 ONLY — the `auth_audit_log` table (layer 2, needs a migration) and `source_ip` capture (needs `Request` threaded through 10+ call sites) are both hard-fenced OUT. Production-touch none: additive observability only, every auth decision/return/raise byte-for-byte identical (brief carries a STOP-if-you-touch-control-flow boundary). Drift: issue cited `dependencies.py:19-44`, the actual 401 is at lines 60-61. NOTE: file-overlaps the still-open #55/#56 PRs on `dependencies.py`, `auth.py`, `test_auth.py` — whichever merges last will conflict; resolve with `/update-pr`.
- [fix-issue-62.md](fix-issue-62.md) — #62: constant-time verify_code comparison via `hmac.compare_digest` in `educator_service.py`. Near-one-line fix plus one stdlib import; brief corrects the stale line number (issue says 336, now 361) and scopes out the DB-side `confirm_token` WHERE clause and the never-compared `access_token`; existing tests `test_wrong_code_rejected`/`test_valid_code_extends_access_and_clears_code` are the regression net.
- [fix-issue-44.md](fix-issue-44.md) — #44: drop the dead `webhook_id` parameter from `PayPalService.verify_webhook_signature` (+ its one caller in `webhooks.py`) and the unused `OrderStatus.PARTIALLY_PAID` member. Three grep-verified line deletions; brief settles the load-bearing DB-enum question (orders.status is `varchar(20)` with no Postgres ENUM/CHECK, so removal is Python-only, no migration) and locates targets by identifier since line numbers drifted.
- [fix-issue-63.md](fix-issue-63.md) — #63 normalize educator emails (case-insensitive); HELD at gate 3 (production-touch: PII data migration)
- [fix-issue-46.md](fix-issue-46.md) — #46: two DRY cleanups — dedupe availability query (`is_slot_available` → thin wrapper over `get_blocking_reason`) + config-drive the 48h payment-link expiry via `_get_config_value` (mirrors `orders.py:66-69`). Production-touch none (defaults to "48" to preserve behavior). Drift: lines 417/703 not 419/705; archived seed sets key to "24" but is off Alembic's path. Bundled booking-flow change — no overlap with #63/#65.
- [fix-issue-65.md](fix-issue-65.md) — #65: prune long-expired `magic_links` via a new standalone `backend/scripts/prune_magic_links.py` + test, mirroring `cleanup_tags.py`. DELETE predicate locked to `expires_at < utcnow() - 30d` (NOT `used_at` — ADMIN_ACCESS links are reusable); naive-UTC cutoff is the key trap. Two new files only; no model/migration/endpoint/cron. Production-touch none. No overlap with #46/#63.
- [fix-issue-83.md](fix-issue-83.md) — #83: narrow `image_storage.generate_thumbnail`'s bare `except Exception` to `(UnidentifiedImageError, Image.DecompressionBombError, OSError)` + `add_breadcrumb`(structured ctx)+`capture_exception`, preserving the `return None` contract; unexpected types now propagate. Drift (shape #2): issue's `capture_exception(e, extras={...})` kwarg is INVALID in sentry-sdk 2.x and used nowhere — brief replaces with the in-repo `add_breadcrumb(... data={...})` + bare `capture_exception(e)` idiom (canonical: `media_scoring.py:145-153` + `educator_service.py:158-166`); line numbers stale (catch at 139-141 not 133-135, caller at 167). Adds a pure-bytes `TestGenerateThumbnail`. Production-touch none. No overlap with #64.
- [fix-issue-64.md](fix-issue-64.md) — #64: add `verify_code_attempts`(int, NOT NULL default 0) + `verify_code_last_attempt_at`(nullable datetime) to the `educators` table (model + new raw-SQL alembic `0004_` revision, down_revision `0003`), unblocking #53's rate-limiting. `verify_code_attempts_max` Settings constant + the logic itself fenced OUT to #53. HELD AT GATE 3 — production-touch YES (schema migration of the `educators` PII/auth table); brief-agent verdict `tractable: no`. Not auto-dispatched; needs operator override (agent authors file artifact only, never applies migration to prod).
