# Issue templates (to lift)

Placeholder. `repo-bootstrap` copies these into the target's
`.github/ISSUE_TEMPLATE/`. Lifted from the pilot project in the roadmap:

- `agent_task.yml` — work scoped for autonomous agent execution (labels: `agent-friendly`).
- `code_quality.yml` — code-quality findings (severity label applied at triage).
- `bug_report.yml`, `feature_request.yml` — standard.
- `config.yml` — template chooser config.

When lifting, genericize any PIC-specific references (e.g. the link to the
agent-friendly criteria doc points at wherever `methodology-install` placed it).
