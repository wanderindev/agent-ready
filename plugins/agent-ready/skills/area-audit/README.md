# area-audit (to lift)

Placeholder. The `area-audit` skill is lifted from the pilot project in the
**lift & sanitize** roadmap step (`SKILL.md` + `references/`). When lifted,
`references/pic-worked-examples.md` must have its `PIC-WORKED-EXAMPLE` blocks
either genericized or moved into the case study — the skill itself ships
codebase-agnostic.

Source of truth until lifted: the pilot repo's `.claude/skills/area-audit/`.
