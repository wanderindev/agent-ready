# fix-issue (to lift)

Placeholder. The `fix-issue` skill is lifted from the pilot project in the
**lift & sanitize** roadmap step (`SKILL.md` + `references/`). It encodes the
brief-writing → gated → implementation-agent pipeline and the non-skippable
verification + production-touch gates.

When lifting, genericize PIC-specific paths (e.g. the outcomes-log location, the
`docs/agent-fixes/` prompt-preservation directory) into configurable defaults.

Source of truth until lifted: the pilot repo's `.claude/skills/fix-issue/`.
