# update-pr (to lift)

Placeholder. The `update-pr` skill is lifted from the pilot project in the
**lift & sanitize** roadmap step. It brings an open PR up to date with the
default branch by merging (never rebasing — force-push is denied), unions
append-only docs automatically, and stops-and-surfaces real source conflicts.

When lifting, genericize the project's append-only-docs union list into a
configurable default.

Source of truth until lifted: the pilot repo's `.claude/skills/update-pr/`.
